;;; window-divider.el --- Window divider configuration

;;; Commentary:
;; Window divider can be used to have a spacing between multiple windows within a frame

;;; Code:

(window-divider-mode t)
(setq window-divider-default-right-width 6)

;;; window-divider.el ends here
